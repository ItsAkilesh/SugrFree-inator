<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SugrFree.Pages.SugrFreePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SugrFree.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <Grid>
        <ScrollView VerticalScrollBarVisibility="Auto">


            <StackPanel>
                <TextBlock
        x:Name="PageHeader"
        Margin="20,30,36,0"
        Style="{StaticResource TitleTextBlockStyle}"
        Text="SugrFree" />
                <StackPanel VerticalAlignment="Top" Orientation="Horizontal">
                    <Button x:Name="ClearButton" HorizontalAlignment="Right" Click="ClearButton_Click" Content="Clear ❌" Margin="20,20,0,0" Style="{ThemeResource DefaultButtonStyle}"/>
                </StackPanel>
                <Grid VerticalAlignment="Bottom" HorizontalAlignment="Left">
                    <Border Margin="20,20,0,0" CornerRadius="9.5" MinWidth="820" Background="{ThemeResource LayerOnMicaBaseAltFillColorSecondary}">
                        <StackPanel Margin="20,20,0,20"  Orientation="Vertical" HorizontalAlignment="Stretch">
                            <TextBlock Margin="0,0,0,5" FontWeight="Bold" Text="Talk with SugrFree AI"/>
                            
                            <ScrollViewer x:Name="ConversationScrollViewer" VerticalScrollBarVisibility="Auto" MaxHeight="500">
                                <ItemsControl x:Name="ConversationList" HorizontalAlignment="Left" Width="750" Margin="0,0,0,20">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="5" Foreground="{Binding Color}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="0,0,8,0" x:Name="AttachButton" HorizontalAlignment="Left" Click="PickAFileButton_Click">
                                    <Button.Content>
                                        <FontIcon Glyph="&#xE723;" />
                                    </Button.Content>
                                </Button>
                                <TextBox x:Name="InputTextBox" HorizontalAlignment="Stretch" KeyDown="InputTextBox_KeyDown" TextWrapping="Wrap" Width="700" MaxWidth="700"/>
                                <Button Margin="10,0,20,0" x:Name="SendButton" Content="Send" Click="SendButton_Click" HorizontalAlignment="Right"/>
                                
                            </StackPanel>
                            <TextBlock x:Name="PickAFileOutputTextBlock" TextWrapping="Wrap" FontSize="9" Padding="0,10,0,0"/>
                            <TextBlock Text="GPT-4o can make mistakes. Always verify important information independently to ensure accuracy." Padding="0,10,0,0" FontSize="10" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                </Grid>



            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
